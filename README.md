# aoc

To install bun

```bash
# linux & mac
curl -fsSL https://bun.sh/install | bash
# window
powershell -c "irm bun.sh/install.ps1 | iex"
```

To install dependencies:

```bash
bun install
```

To run test:

```bash
bun test --watch
```
